CSS.m is the script file that holds the main algorithm.

Change the filename of the image read in at line 3 to perform the algorithm on different samples.

Code to diplay figures for different steps of the algorithm are included but commented out, you may change it to display a differnet figure, but only one part at a time, and many figures are meant for only one sigma level.